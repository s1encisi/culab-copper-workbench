# CuLab 面试问答与简历表述

这份材料围绕一个核心价值组织：把工业研究中的开放式问题，转化为有证据、可复核、可审批的工程流程。先用简答建立项目价值，再按面试官追问展开技术细节。数字口径以代码目录与验收记录为准，设备案例来自 Mock。

## 一分钟项目介绍

我围绕铜电解电积研究构建了 CuLab 智能体工作台。工艺研究中，历史事件、预测模型、优化结果和规程文档往往分散在不同工具里，结论难以追溯，研究建议也缺少通向受控执行的明确流程。

CuLab 让研究智能体按问题查询只读工具，把结果绑定到数据、模型和文档版本；涉及模型发布或设备执行时，由独立服务检查权限、参数哈希和状态版本，再进入人工审批与结果回读。项目接入了 38 个预测方法接口、26 个优化方法接口，并提供无需工厂数据和 API 密钥的公开合成演示。

我希望面试官看到的核心能力，是把智能体的不确定推理与工业任务需要的确定性计算、权限和恢复机制组织成可验证的系统。

## 1. 这个项目具体解决什么问题？

**简洁回答：** 解决工业研究中“资料分散、结论难复核、建议难以受控执行”的衔接问题。用户可以从历史事件出发，完成证据查询、模型或优化结果分析，并在需要时进入明确审批的执行流程。

**可展开的技术细节：** 输入快照记录当时可用的信息；工具结果有 evidence_id；模型工件与文档引用绑定版本和哈希；执行记录与设备账本独立保存，便于追踪整个决策链。

## 2. 你本人贡献了什么？如何使用 AI？

**简洁回答：** 我的贡献集中在研究场景与交付目标的明确、方案取舍和验收要求的组织。工程实现采用 AI 辅助开发，成果通过代码、测试和运行记录交付。

**可展开的技术细节：** 我提出并坚持的交付要求包括：保留升级前基线、按阶段记录依赖、对已实现功能先复现问题再修复、公开前检查完整历史并保护私密资料。面试时应结合自己能够现场解释和复现的模块展开，实际编码分工、协作者和投入时间按真实经历说明。

## 3. 为什么这里需要智能体？

**简洁回答：** 工艺研究问题经常需要先确定资源和分析路径，再根据中间证据决定下一步，适合由智能体组织工具查询。训练、求解和执行则走明确的服务接口。

**可展开的技术细节：** 研究循环可以查询资源、读取证据、选择分析工具并完成带引用的回答；工具步数、调用数和预算受到约束。这样既保留开放问题的处理能力，也让关键计算与写入行为可审查。

## 4. 这是单智能体还是多智能体？

**简洁回答：** 当前交付主路径是一个研究智能体与多个职责明确的工程服务协作。项目保留历史编排实验代码，主路径以 research_service.py 为准。

**可展开的技术细节：** 研究、数据、计算、知识、审批和执行有清楚的接口边界。后续是否增加独立模型角色，取决于同预算下的任务成功率、延迟、费用和稳定性对照，而不是角色数量。

## 5. 项目的核心创新点是什么？

**简洁回答：** 核心价值在于把工业研究所需的时序证据、版本化知识、模型资格和受控执行统一到一条可追踪流程中。

**可展开的技术细节：** 三个值得展示的机制是：按可得时间构造研究证据；以本地事实引用和文档授权约束回答；把具体参数审批、幂等交付与连续回读结合起来。它们分别对应研究有效性、结论可复核性和执行可靠性。

## 6. 为什么采用这些技术？

**简洁回答：** FastAPI 提供明确的服务接口，React/TypeScript 承载研究工作台，SQLite 保存任务、权限和审计状态，成熟建模与优化库提供可复用算法能力。

**可展开的技术细节：** 项目通过方法注册与工件合同隔离算法差异。研究主路径采用有界工具循环和持久化任务记录；历史 LangGraph 实验实现单独保留，避免把实验框架与当前运行路径混为一谈。

## 7. 最关键的工程难点是什么？

**简洁回答：** 最关键的是让数据时间、研究结论和执行状态在异常情况下仍保持一致。

**可展开的技术细节：** 化验可能延迟可得，文档会更新，模型会替换，通信 ACK 也可能丢失。系统分别通过时间快照、引用重验、版本绑定和设备账本协调这些变化，而不是让一段生成文本承担所有状态。

## 8. 如何避免时间泄漏和不公平比较？

**简洁回答：** 在固定时序协议和共同评价样本上比较，明确输入与标签何时可用，将拟合、校准和外层验证分开。

**可展开的技术细节：** 预处理和模型拟合使用对应训练段；校准不借用最终验证标签。结果记录方法、种子、来源和指标，资格判定保留预先声明的种子范围，不用单次最好结果替代整体结论。

## 9. 38 个模型和 26 个优化器代表什么？

**简洁回答：** 代表统一接口下的研究方法覆盖范围。系统还分别记录训练、比较、影子验证和发布资格。

**可展开的技术细节：** 不同方法具有不同适用域、资源成本和不确定性表达。平台价值是让它们在明确协议下被选择、复核和管理，方法数量不能直接换算成业务收益或上线数量。

## 10. 如何证明项目有效？

**简洁回答：** 让每类主张都有对应证据：预测与优化看固定协议评价；智能体看任务、引用和调用记录；执行机制看审批与故障注入结果。

**可展开的技术细节：** 上一轮有 74 个独立自动测试用例通过；本轮有 17 项专项回归通过。公开演示中，正常和 ACK 丢失场景均完成验证，设备写入次数均为 1。模型准确率、真实调用质量和现场收益使用各自的评价口径。

## 11. 怎么防止智能体越权操作？

**简洁回答：** 研究工具只读，写入权限由服务端身份和独立审批服务决定。

**可展开的技术细节：** 请求正文、文档内容和会话摘要不能赋予权限。审批绑定具体 payload_hash、版本、有效期和发起者状态；执行前再次检查条件，参数或状态发生变化时原审批失效。

## 12. ACK 丢失为什么不能直接重发？

**简洁回答：** ACK 丢失意味着结果未知，设备可能已经接收命令。系统先查询设备账本并核对反馈，再确认结果。

**可展开的技术细节：** outbox 保存交付意图，命令编号用于幂等，执行租约隔离旧执行者。公开演示保留 ACK 丢失但最终 VERIFIED 的记录，并证明设备只写入一次。

## 13. 如何处理任务中断和恢复？

**简洁回答：** 把任务、证据、调用结果和预算持久化，恢复时确认来源与执行权，不把进程重启等同于从头重做。

**可展开的技术细节：** 已知结果可以复用，未知费用不能自动当成零并重试；旧执行者不能提交新结果。对已经停止的比较/校准进程，查询接口显示中断状态，同时保留原始记录和工件。

## 14. RAG 和会话记忆有什么特点？

**简洁回答：** 文档知识管理版本和授权，会话记忆保留可重建的引用，关键事实回到权威来源读取。

**可展开的技术细节：** 文档片段绑定内容与解析哈希、可得时间和 ACL。正文进入外部模型需要明确授权；撤销或过期会影响引用和派生回答。数值和单位可由本地证据占位符回填，避免把摘要当作新的事实源。

## 15. 文档里包含恶意指令怎么办？

**简洁回答：** 文档作为证据处理，不能改变工具注册表、角色权限或审批条件。

**可展开的技术细节：** 工具参数经过结构校验，访问范围由服务端确认，文档正文进入模型的范围受授权约束。测试覆盖文档尝试增加控制工具、跨用户访问和撤销时仍在生成回答等场景。

## 16. 如何控制调用成本？

**简洁回答：** 使用有界调用、证据复用、费用预留和结果核对，让成本成为任务执行的一部分。

**可展开的技术细节：** 系统记录模型/工具调用、token、缓存和耗时；费用未知的请求保留预留，避免无依据重试。性能评估应同时看任务成功率、p50/p95、失败率和费用，公开离线演示的真实模型调用数为 0。

## 17. 目前的落地范围是什么？

**简洁回答：** 已交付历史研究工作台和 Mock 执行闭环，可以验证从证据到审批、再到设备反馈的工程流程。

**可展开的技术细节：** 现场试点需要接入设备通信、联锁、网络边界和操作授权。已有的身份、审批、幂等与回读机制可以沿用，现场效果再通过明确的工况与业务指标评估。

## 18. 如果候选模型没有达到采用门槛，怎么处理？

**简洁回答：** 保留当前已批准模型，记录候选的完整证据，按同一资格标准决定下一步。

**可展开的技术细节：** 采用决策看共同样本、误差、覆盖、资源成本和适用工况。登记和训练并不自动改变发布指针，平台通过这一分离避免把方法复杂度当作采用理由。

## 19. 如何保证仓库公开时的数据安全？

**简洁回答：** 将可公开源码和合成示例与私密资料分开，检查完整 Git 历史和远端附件，并在本地加密备份敏感资料。

**可展开的技术细节：** 阶段分支通过明确引用同步；本地备份引用不推送。Excel、行级数据、模型工件、内部文档和凭据不进入公开仓库。恢复密钥由 Windows 当前账户保护，原件保留，归档逐文件解密验证。

## 20. 后续最值得投入的方向是什么？

**简洁回答：** 用真实错误类型决定下一步投入：知识不足改进检索与资料治理，稳定的格式或工具行为问题再评估领域适配，现场需求成熟后推进受控试点。

**可展开的技术细节：** G7 的前置条件是审核样本、冻结对照集、资源方案与预算。增加模型角色、做 SFT 或继续长时间实验，都应回答一个明确的研究或业务问题，并设定可测的验收条件。

## 简历项目表述示例

**CuLab 铜电解电积研究智能体工作台**

- 围绕工业研究的证据查询、模型分析与受控执行需求，构建研究智能体与数据、计算、知识、审批服务协作的工作台，统一接入 38 个预测方法和 26 个优化方法接口。
- 实现版本化证据引用、任务恢复、调用预算、文档授权与模型生命周期；以具体参数哈希、状态版本和人工审批约束执行流程。
- 提供正常执行及 ACK 丢失恢复的公开合成演示，验证单次设备写入与连续反馈；通过自动测试和前后复现记录验证工程约束，采用 AI 辅助开发形成可追踪交付。

项目描述可依据岗位侧重调整；个人职责、实际团队分工及现场经历以本人真实情况为准。
