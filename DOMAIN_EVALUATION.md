# G7：以小预算验证领域资料的作用

领域适配工作区已接入实际研究服务：保存问题与资料，比较 Prompt 与 RAG 两种条件，查看回答、引用、调用量、费用和复核结论。沿用原有任务、知识库与预算记录。

## 使用流程

1. 在「领域适配」填写问题，每行一题，最多四题；填写本次可使用的资料与来源。
2. 保存方案后查看资料。该步骤在本地登记文本、建立索引。
3. 配置真实模型后点击「运行对照」。每题运行两种条件，总上限 2 元、每任务最多 0.5 元。
4. 查看实际回答和失败原因，填写复核。失败案例保留，其他案例继续；重复打开或运行已结束方案不重复计费。

默认合成资料说明 Cu/As 单位、Mock ACK 和连续回读规则。方案与文档版本、研究配置绑定；不同用户的方案分别管理。

## 2026-09-16 真实调用记录

DeepSeek Flash；两道相同问题 × 两种条件；每任务最多 4 次模型调用、4 次业务工具调用、2048 个输出 token。用户批准总预算 2 元，实际记账 **0.26662056 元**，未结算预留为 0。

| 条件 | 任务数 | 生成回答 | 文档片段引用数 | 模型调用数 | 业务工具调用数 | 费用 / 元 | p50 / 秒 | p95 / 秒 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 当前提示词 | 2 | 0 | 0 | 8 | 8 | 0.13100624 | 17.206 | 18.153 |
| 提示词 + 合成资料 | 2 | 2 | 3 | 6 | 5 | 0.13561432 | 11.930 | 12.099 |

RAG 回答给出了 Cu 使用 g/L、As 使用 mg/L，以及 ACK 后仍需三个连续合格回读、ACK 丢失先查账本的关键规则。第一题把单位倍率称为“量纲差”，开发复核记录了这一措辞问题。

两条 Prompt 任务均达到调用次数上限。轨迹发现 project_status 返回了完整方法配置，单次请求输入达到 28,425 token。随后将其改为方法标识、登记状态和目标单位的摘要，完整配置仍由原方法目录提供。修复使用本地回归验证，没有追加付费重测。

以上是两道合成问题的工程对照。p50/p95 是本次小样本的描述值；本次结果支持继续使用资料检索路线，不据此宣称一般任务的准确率或加速比例。

## 实现与验证

- 服务：src/copper_mvp/domain_evaluation.py；接口：/api/v2/domain-evaluations。
- 页面：web/src/DomainWorkspace.tsx。
- 测试：tests/mvp/test_domain_evaluation.py 与 test_g3_research.py，7 项通过。
- 前端：TypeScript 与生产构建通过；浏览器检查覆盖导航、表单、真实结果、失败原因与已结束任务禁用重跑。
- 合成/替身与真实模型调用分别记录。实际评测摘要见 [验收记录](examples/domain_evaluation_20260916.json)。

G7 本轮交付 Prompt/RAG 方案与评测闭环。权重微调留给已积累足够审核样本、且现有检索仍无法解决的稳定错误；本轮没有启动 SFT/LoRA。
